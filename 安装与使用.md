# 六艺日课 · 私人健身记录

这是独立的自托管版本，与文件夹中的旧 Sites、妙搭版本分开。前端为轻量 PWA，服务为 Node.js 24 + SQLite，无需安装应用依赖。所有动作照片都在安装包内。

## Windows 先用起来

1. 安装 Node.js **24 LTS（24.14或更新的24.x）**。这台开发电脑已有兼容版本。
2. 打开本目录，运行 `启动-Windows.ps1`；也可在终端运行：

   ```powershell
   node --env-file-if-exists=.env server.mjs
   ```

3. 浏览器打开 `http://localhost:4177`，首次创建管理员账号并设置6位数字密码。
4. 选择每组实际完成次数并勾选“完成”，最后保存。没有完成的组不会计入记录。

默认起点：墙壁俯卧撑2×30、折刀深蹲1×10、垂直引体2×20、坐姿屈膝1×2。这些是初始计划，不是已经发生的训练记录。每天按量训练。前四艺达到第六式后，可在计划里开启桥和倒立撑。

## 交付内容

- 今日训练、逐组记录、休息记录、备注与身体状态。
- 60式动作卡、六艺原长图、可编辑的三级参考标准。
- 历史日历、补记和修改、删除前备份、连续训练天数、每月训练日与分单位总量。
- 计划与等级调整；历史记录保留当时的式数，等级变化有时间记录。
- JSON完整导出、CSV逐组导出、JSON合并导入（跳过已有日期，可选择恢复计划）。
- SQLite每日在线一致性备份，最近30份保留；导入、删除前也会备份。备份包括登录信息，应和个人数据一起保管。
- PWA主屏幕安装、离线页面和本机草稿。仅勾选并保存才进入服务端；离线不自动提交，恢复网络后手动保存。不要清理浏览器数据或退出登录前丢弃尚未保存的草稿。
- 多用户独立账号登录，会话30天；同一天、同一计划的跨设备编辑会检查版本冲突。

推送、语音、自动进阶建议、Apple健康接入留待下一版。已有Codex每天07:30提醒属于Codex任务，迁到Mac后不会自动变成网页后台推送。

## 把数据和应用迁到Mac mini

安装包不包含Windows上的个人数据库。若已经在Windows记过训练，先从网页导出JSON，再在Mac导入。也可停掉Windows服务后完整复制 `data` 目录到Mac，这样保留密码与完整等级变更历史。

先把整个 `fitness-tracker` 文件夹放到Mac的固定位置，例如 `~/Applications/fitness-tracker`。图片已包含在 `public/images`，不依赖Windows路径。

### 方案A：Docker（原方案）

1. Mac安装并启动Docker Desktop，将其设为用户登录后启动。
2. 终端进入本目录，复制配置后，在 `.env` 的 `APP_INITIAL_PASSWORD=` 后填写自己选定的6位数字密码。它只在空数据库首次初始化时生效：

   ```bash
   cp .env.example .env
   # 编辑 .env 并设置 APP_INITIAL_PASSWORD 后，再启动：
   docker compose up -d --build
   ```

3. 在Mac浏览器打开 `http://localhost:4177`，用账号名 owner 和刚设置的密码登录。成功后从 `.env` 清空 `APP_INITIAL_PASSWORD`，运行 `docker compose up -d --force-recreate`。数据库中已有的密码不会被覆盖。Docker方式无需在Mac额外安装Node。

4. `data` 和 `backups` 绑定到安装目录，更新镜像不会移除这些数据。容器随Docker重启而启动；Mac重启后通常需要登录用户、Docker启动，服务才恢复。请在系统设置中保持Mac接电时不自动睡眠。关机或睡眠期间无法访问。

镜像以Node 24官方多架构基础镜像构建；同一份代码用于Intel和Apple Silicon，需在目标Mac首次构建。当前Windows环境没有验证Docker和Mac启动结果。

### 方案B：直接运行（不需要Docker）

1. 在Mac安装Node.js 24 LTS。
2. 终端进入本目录，运行 `bash 启动-Mac.command`，打开网页完成设置并验证。
3. 按Ctrl+C停止手动进程，再运行：

   ```bash
   node scripts/install-launchd.mjs
   ```

此配置在当前Mac用户登录后自动运行并在服务退出时重启。它不是系统级开机守护进程；重启后需要用户登录。日志在 `data/service.log` 与 `data/error.log`。后续移动安装目录或更换Node安装路径，需要更新配置。

停用：

```bash
launchctl bootout gui/$(id -u) ~/Library/LaunchAgents/local.sixarts.fitness.plist
```

不要同时启动Docker和原生服务，它们会争用同一端口。

## 手机私人访问与HTTPS

1. Mac和手机安装Tailscale，登录同一个私人网络。
2. 在Mac上运行：

   ```bash
   tailscale serve --bg http://127.0.0.1:4177
   ```

3. 复制Tailscale实际返回的 `https://…ts.net` 地址。在 `.env` 中把 `APP_ORIGIN=` 填为该地址，不带末尾 `/`，然后重启健身服务。Docker用 `docker compose up -d --force-recreate`；原生自启动用 `launchctl kickstart -k gui/$(id -u)/local.sixarts.fitness`。
4. 手机上保持Tailscale连接，用Safari打开该HTTPS地址并登录，分享 → 添加到主屏幕。安卓可从浏览器菜单安装。

Tailscale Serve只向私人网络提供服务。私人访问不需要公网开放、域名购买或Funnel。如果手机需要不安装客户端、通过移动网络直接访问，可选择 [Mac 与 Funnel 部署](Mac与Funnel部署.md) 中的公网方案。HTTPS用于手机PWA和安全Cookie。配置HTTPS后请主要从HTTPS入口登录；本机HTTP入口不能发送Secure Cookie。

朋友也需要获得私人网络的访问权限，且在自己的设备连接该网络；应用邀请码只用于创建应用账号，不自动授予网络访问权限。

若Tailscale Serve代理覆盖Host而不保留原地址，服务仍接受localhost后端Host，但浏览器请求来源必须与APP_ORIGIN一致。不要把4177端口直接映射到公网。

官方参考：[Tailscale Serve](https://tailscale.com/docs/features/tailscale-serve)、[Node SQLite](https://nodejs.org/docs/latest-v24.x/api/sqlite.html)。

## 备份、恢复和更新

每天运行时自动生成一致的 `.sqlite` 快照，间隔一小时检查一次。保留最近30份。应将 `data`、`backups` 和安装目录加入Time Machine或复制到另一个硬盘。保留安装包即可保留全部动作图片。

一般恢复：网页导入JSON，只添加缺失日期，不覆盖已有日期；可选择恢复当前计划。JSON中包含等级变更历史用于留档，但合并导入不会重放旧的等级变更事件，只记录本次计划恢复造成的等级变化。

完整数据库恢复（撤销误修改或恢复所有历史）：

1. 停止服务（Docker停止容器，原生停止手动或launchd进程）。
2. 将整个 `data` 目录改名保留，例如 `data-before-restore`，新建空 `data`。
3. 从 `backups` 选一个快照，复制到新 `data` 目录并重命名为 `fitness.sqlite`。不要把旧的 `-wal`、`-shm` 文件混入新目录。
4. 重新启动服务。快照中的密码和登录会话也会恢复；如需重新登录，停止服务后执行 `node --env-file-if-exists=.env scripts/reset-password.mjs --confirm`，再启动并在本机设置新密码。

升级：先网页导出JSON并生成备份，再停止服务，替换代码、`lib`、`public`，保留 `.env`、`data`、`backups`。如修改前端离线资源，开发者须递增 `public/sw.js` 的CACHE版本。Docker重新构建；原生重新启动。

旧Sites/妙搭数据不会被自动读取。本版不擅自迁移远端记录；需要时另行提供导出文件并做格式转换。

## 验证

```bash
node scripts/check.mjs
node --test test/*.test.mjs
```

测试使用独立临时数据库，不修改真实训练记录。真实Mac设备、Tailscale网络和iPhone安装仍需在目标设备验证。

## 2026-09-11 更新：选择动作与分享仪表盘

今日训练卡片可直接下拉选择式；“同一艺再练一式”会新增独立卡片。新增卡片从未添加的第一式开始，可再选择需要的式，填写完成量并勾选保存。变更已有完成组的动作会先提示确认，不会自动改变长期训练计划。

侧栏“仪表盘”可选截至日期并下载PNG图片。包含飞书同口径综合强度、六艺雷达图、最近30天趋势、当日训练汇总。每艺评分取截至日期最近7日最佳阶段加权值，再对六艺求平均；无记录计0。评分使用2026-09-11读取的飞书标准快照，不受图解中自定义参考值影响。分享图片不含身体状态、备注和账号信息。

## v1.1 手机布局更新

手机仪表盘的四个指标采用两行两列布局，雷达图、各艺今日训练得分、综合强度趋势、六艺水平增长曲线依次单列显示。当日训练汇总使用可读的动作卡片，列出全部组数与合计。趋势纵轴随数据调整。分享图片通过预览弹窗下载，也可在手机长按保存。电脑和导出图片保留完整报告排版。

从GitHub下载或使用ZIP部署包均可安装。GitHub仅存放应用代码，运行仍由Mac mini负责。请勿上传data、backups或.env。Mac需重新配置访问地址，不能沿用Windows手机热点地址。手机和电脑已保存的训练数据通过JSON导出/导入迁移；尚未保存的浏览器草稿不会随部署包迁移。

## v2.0 多用户升级与备份

原v1.x数据自动归入owner管理员账号，旧密码保持不变；首次迁移前自动创建备份。每位成员使用独立数据库，网页接口从当前会话选择数据空间，不接受客户端指定另一个人的空间。浏览器草稿按账号隔离。登录前不会从本地缓存展示训练数据，因此离线冷启动需要先恢复网络登录；已登录的页面断网后仍可保留草稿，恢复连接再保存。

管理员在“计划与数据”生成邀请码，朋友在登录页选择“邀请加入”。账号名为3至32位字母、数字、下划线或短横线，密码保持6位数字。邀请码24小时有效且只能使用一次。管理员可查看账号列表并生成密码重置码，不能通过网页查看其他用户训练数据。服务器所有者可直接访问磁盘数据。

管理员数据库仍为data/fitness.sqlite，同时包含账号登记、会话和邀请码哈希。成员训练数据库位于data/users/<账号ID>/fitness.sqlite，对应自动备份在backups/users/<账号ID>/。每日备份分别进行，不是跨所有数据库的同一时刻快照。完整灾难恢复与迁移需先停止服务，复制整个data目录及所有子目录；建议同时复制backups。只复制管理员数据库会遗漏成员训练记录。单个用户的网页JSON导出不包含其他用户记录或登录信息。

管理员丢失密码时，先停服，按上文执行scripts/reset-password.mjs；v2.0只重置管理员账号，不删除成员账号或任何训练记录。重新启动后在服务器本机创建管理员账号。首次设置与本机恢复入口仅在管理员不存在时开放。不要用v1.x程序启动已经升级的多用户数据目录；回退需使用升级前备份。

新增自动检查覆盖两用户同日期记录、计划/仪表盘/导入导出/删除隔离、非管理员权限、一次性邀请、邀请过期、密码重置和会话失效、登录限流、旧数据与旧会话迁移、成员独立备份以及浏览器草稿隔离。17项测试全部通过；Mac设备和多手机真实访问仍待部署验收。
