# Local start: no changes needed. For a private HTTPS URL, set the exact origin (no trailing slash).
HOST=127.0.0.1
PORT=4177
APP_ORIGIN=
# Optional: initial password for the FIRST container start. Remove after setup; existing passwords are never overwritten.
APP_INITIAL_PASSWORD=
